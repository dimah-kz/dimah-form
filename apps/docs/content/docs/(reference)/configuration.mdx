---
title: Configuration
description: Options for dimahForm(), createFormClient(), useFormResponse, and ResponseStore.
icon: Settings
---

## dimahForm()

`database` is required. `@dimah-form/db` is not — `memoryAdapter()` ships in `@dimah-form/server`.

```ts
import type { DimahFormConfig } from "@dimah-form/server";
```

<AutoTypeTable
  path="packages/server/src/dimah-form.ts"
  name="DimahFormConfig"
/>

### guard

```ts
import type { DimahFormGuardContext } from "@dimah-form/server";
```

<AutoTypeTable
  path="packages/server/src/types.ts"
  name="DimahFormGuardContext"
/>

### hooks

`on*` runs after validation and before the write. `after*` runs after persist — keep side effects there.

```ts
import type { DimahFormHooks } from "@dimah-form/server";
```

<AutoTypeTable path="packages/server/src/types.ts" name="DimahFormHooks" />

## createFormClient()

```ts
import type { CreateFormClientOptions } from "@dimah-form/react";
```

<AutoTypeTable
  path="packages/core/src/create-form-client.ts"
  name="CreateFormClientOptions"
/>

Pass `createFormClient<Form, typeof plugins>` when you need literal plugin method names.

## useFormResponse()

```ts
import type { UseFormResponseOptions } from "@dimah-form/react";
```

<AutoTypeTable
  path="packages/react/src/use-form-response.ts"
  name="UseFormResponseOptions"
/>

Returned state and actions: [React](/docs/react).

## ResponseStore

Anything passed as `database` implements this. Methods may be sync or async.

```ts
import type { ResponseStore } from "@dimah-form/server";
```

<AutoTypeTable path="packages/server/src/store.ts" name="ResponseStore" />

### ListResponsesStoreQuery

<AutoTypeTable
  path="packages/server/src/store.ts"
  name="ListResponsesStoreQuery"
/>

### StoreWriteOptions

<AutoTypeTable path="packages/server/src/store.ts" name="StoreWriteOptions" />

`memoryAdapter()` is an isolated in-memory store. `db()` from `@dimah-form/db` is the SQL adapter. A custom store is passed to `dimahForm` directly — do not wrap it in `db()`.

## Next steps

<Cards>
  <Card
    title="Protocol"
    href="/docs/protocol"
    description="HTTP paths for the same operations."
  />
  <Card
    title="Persistence"
    href="/docs/persistence"
    description="SQL schema and the FumaDB client."
  />
  <Card
    title="Errors"
    href="/docs/errors"
    description="Codes the store and guard can surface."
  />
</Cards>
