# @dimah-form/react

Thin React client for dimah-form. `createFormClient()` returns the protocol API plus a `Provider`; `useFormClient()` reads it. Pass `plugins` the same way as `@dimah-form/core`. No field widgets — you render answers.
