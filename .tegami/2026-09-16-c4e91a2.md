---
packages:
  group:dimah-form: minor
---

### Add start, draft, and submit protocol

`dimahForm()` now serves get-form, start, get-response, save-draft, and submit. Submit validates against the definition snapshot taken at start, not the live questionnaire. The typed `createFormClient` methods call the same routes.
